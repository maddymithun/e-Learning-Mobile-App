<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.example.composecleanarchitecture.ComposeCleanArchitecture"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_raf_khata"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_raf_khata"
        android:supportsRtl="true"
        android:theme="@style/Theme.ComposeCleanArchitecture"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".base.MainActivity"
            android:exported="true"
            android:label="RafKhata"
            android:theme="@style/Theme.ComposeCleanArchitecture"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>